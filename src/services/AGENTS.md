# services/

Side-effecting helpers. Google Maps, DOM, `fetch`, `localStorage` are all allowed. No React, no JSX.

If a helper is strictly pure (only depends on its inputs), put it in `utils/` instead.
