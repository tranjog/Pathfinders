# CLAUDE.md

## README

Update README.md on every commit to reflect any changes made — new features, removed features, architectural changes, new dependencies, or new directories under `src/`. Keep the feature list, architecture section, and intro paragraph in sync with the actual codebase.
